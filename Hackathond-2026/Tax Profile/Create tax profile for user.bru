meta {
  name: Create tax profile for user
  type: http
  seq: 1
}

post {
  url: {{API_URL}}/users/:userId/tax-profile
  body: none
  auth: inherit
}

params:path {
  userId: 
}

settings {
  encodeUrl: true
}
